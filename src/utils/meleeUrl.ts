export const playerControllerUrl = (tournamentId: string) =>
  `https://melee.gg/Player/Control/${tournamentId}`

export const playerPortalUrl = (playerGuid: string) =>
  `https://melee.gg/Player/Portal/${playerGuid}`
