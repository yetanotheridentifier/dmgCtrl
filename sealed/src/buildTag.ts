/**
 * Visible build marker — bump on every meaningful change so "which code is this
 * browser running?" is answerable at a glance. Shown bottom-right in dev, and at
 * the foot of the Help page in prod (#332).
 */
export const BUILD_TAG = 'b56'
