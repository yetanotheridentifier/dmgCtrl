{
  "name": "swu-proxy",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.6",
    "vitest": "^4.1.6"
  }
}
